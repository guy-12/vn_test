layeredimage augustina:

    always:
        "augustina_base"

    group eyes:

        pos (95, 90)

        attribute open default
        attribute wink

    group eyebrows:

        pos (100, 85)

        attribute normal default
        attribute one_raised:

            pos (100, 70)
